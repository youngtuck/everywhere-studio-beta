/** Scoped typography for marketing legal pages (Terms, Privacy, Cookies). */
export const LEGAL_DOC_TYPOGRAPHY_CSS = `
.xp-legal-doc h1 {
  font-size: 36px;
  font-weight: 600;
  letter-spacing: -0.03em;
  margin: 0 0 8px;
  color: var(--xp-text);
}
.xp-legal-doc .xp-legal-doc-operator {
  font-size: 13px;
  color: var(--xp-ter);
  margin: 0 0 48px;
}
.xp-legal-doc h2 {
  font-size: 18px;
  font-weight: 600;
  margin: 40px 0 12px;
  color: var(--xp-text);
}
.xp-legal-doc p {
  font-size: 15px;
  line-height: 1.7;
  color: var(--xp-sec);
  margin: 0 0 16px;
}
.xp-legal-doc ul,
.xp-legal-doc ol {
  font-size: 15px;
  line-height: 1.7;
  color: var(--xp-sec);
  padding-left: 20px;
  margin: 0 0 16px;
}
.xp-legal-doc table {
  width: 100%;
  border-collapse: collapse;
  font-size: 13px;
}
.xp-legal-doc th {
  text-align: left;
  padding: 8px 12px;
  border-bottom: 2px solid var(--xp-border);
  font-weight: 600;
  color: var(--xp-text);
}
.xp-legal-doc td {
  padding: 8px 12px;
  border-bottom: 1px solid var(--xp-border);
  color: var(--xp-sec);
}
.xp-legal-doc strong {
  font-weight: 600;
  color: var(--xp-text);
}
.xp-legal-doc hr {
  border: none;
  border-top: 1px solid var(--xp-border);
  margin: 40px 0;
}
.xp-legal-doc a {
  color: var(--xp-blue);
  text-decoration: underline;
}
`;
