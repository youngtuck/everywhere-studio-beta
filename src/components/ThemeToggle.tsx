/**
 * Theme switching was removed; the app uses light mode only.
 * Kept as a stub so older imports do not break.
 */
const ThemeToggle = (_props: { onDark?: boolean }) => null;
export default ThemeToggle;
